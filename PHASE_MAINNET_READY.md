# 主网上线准备清单 (Phase 27-31 验证)

## 📋 概述

本文档列出了主网上线前必须完成的压测和验证项目，确保 Phase 27-31 的机制能够有效防止：
- 同一设备多浏览器高度/余额不一致
- 用户白挖（挖到无效链）
- 链分叉和状态混乱

---

## ✅ 架构保证总结

### 1. 同一台机器上：高度 & 余额一致性

**对应机制：**
- **Phase 27: LocalInstanceCoordinator**
  - 单机选举 LEADER / FOLLOWER
  - 只有 LEADER 可以挖矿，FOLLOWER 全部只读
  - 角色变化时自动停止非 Leader 实例的挖矿

- **Phase 29: LocalStateCoordinator**
  - LEADER 每秒广播 STATE_UPDATE（height + tipHash + stateCommitment）
  - FOLLOWER 检测不一致时自动同步

**结果：** ✅ 同一台设备多浏览器高度/余额不一致 → 已用强制同步 + 单 Leader 挖矿解决

### 2. 全网层面：不白挖、不乱链

**对应机制：**
- **Phase 30: Mainnet Guardrails**
  - Network handshake 防止连接错误网络
  - MiningGuard 检查挖矿前置条件
  - MiningStatsTracker 监控挖矿有效率

- **Phase 31: 反分叉 + 根节点对齐**
  - LongRangeDetector 检测长程分叉
  - HeightConsensusManager 确保高度一致
  - AntiInvalidMining 防止无效挖矿
  - CheckpointLock 防止重组
  - SignalReconciliation 与根节点对齐

**结果：** ✅ "白挖" / 错链问题已通过多层护栏解决

---

## 🧪 压测场景 Checklist

### A. 多浏览器本机一致性测试

#### A1. 多浏览器同时运行测试
- [ ] **测试步骤：**
  1. 同时打开 Chrome、Edge、Firefox（或同一浏览器的多个标签页）
  2. 所有窗口连接到同一信令服务器
  3. 只在一个窗口（LEADER）手动开启挖矿
  4. 观察其他窗口是否自动变为 FOLLOWER
  5. 运行 30 分钟，每 5 分钟记录一次状态

- [ ] **验证点：**
  - [ ] 所有窗口显示相同的高度
  - [ ] 所有窗口显示相同的余额
  - [ ] 所有窗口显示相同的隐私余额（Note）
  - [ ] 只有 LEADER 窗口在挖矿
  - [ ] FOLLOWER 窗口显示"只读模式"提示
  - [ ] 关闭 LEADER 窗口后，其他窗口能正确选举新的 LEADER

#### A2. 角色切换测试
- [ ] **测试步骤：**
  1. 打开两个窗口，确认一个是 LEADER，一个是 FOLLOWER
  2. 在 LEADER 窗口开始挖矿
  3. 关闭 LEADER 窗口
  4. 观察 FOLLOWER 是否自动变为 LEADER
  5. 新 LEADER 是否自动开始挖矿（如果之前开启了）

- [ ] **验证点：**
  - [ ] 角色切换时没有状态丢失
  - [ ] 挖矿状态正确传递
  - [ ] 高度和余额保持一致

#### A3. 状态同步测试
- [ ] **测试步骤：**
  1. 在 LEADER 窗口进行转账操作
  2. 观察 FOLLOWER 窗口是否立即同步余额变化
  3. 在 FOLLOWER 窗口尝试转账（应该被阻止或提示）

- [ ] **验证点：**
  - [ ] FOLLOWER 能实时看到 LEADER 的状态变化
  - [ ] stateCommitment 验证通过
  - [ ] 余额计算正确

---

### B. 网络抖动 / Signal 不稳定测试

#### B1. Signal 断开重连测试
- [ ] **测试步骤：**
  1. 连接到主网 signal
  2. 开始挖矿
  3. 模拟断开网络连接（或关闭 signal 服务器）
  4. 等待 5 分钟
  5. 恢复网络连接

- [ ] **验证点：**
  - [ ] 断开时挖矿自动停止（MiningGuard 检测）
  - [ ] 重连后能自动同步到最新高度
  - [ ] 不会在错误链上继续挖矿
  - [ ] SignalReconciliation 能正确对齐

#### B2. P2P 网络分区测试
- [ ] **测试步骤：**
  1. 启动多个节点
  2. 模拟网络分区（部分节点无法通信）
  3. 观察各分区节点的行为

- [ ] **验证点：**
  - [ ] 少数派分区自动停止挖矿（HeightConsensusManager）
  - [ ] 恢复连接后能自动同步
  - [ ] 不会产生永久分叉

---

### C. 极端轻节点 + 极端裁剪测试

#### C1. 最小窗口测试
- [ ] **测试步骤：**
  1. 设置 `lightNodeWindow = 1`（最小窗口）
  2. 多次重启节点
  3. 使用 GSN 拉取快照
  4. 验证状态一致性

- [ ] **验证点：**
  - [ ] stateCommitment 验证始终通过
  - [ ] 余额计算正确
  - [ ] 快照加载和验证成功

#### C2. 快照同步测试
- [ ] **测试步骤：**
  1. 清除本地所有区块和快照
  2. 重新连接网络
  3. 观察是否自动从 GSN 拉取快照
  4. 验证快照后的状态

- [ ] **验证点：**
  - [ ] 能正确找到并下载合适的快照
  - [ ] 快照验证通过
  - [ ] 快照后的状态与网络一致

---

### D. 挖矿有效率监控测试

#### D1. 主网挖矿测试
- [ ] **测试步骤：**
  1. 连接到真实主网 signal
  2. 运行挖矿 2-4 小时
  3. 记录所有挖到的区块
  4. 检查 MiningStats 中的有效率

- [ ] **验证点：**
  - [ ] 挖矿有效率接近 100%（偶发孤块可接受）
  - [ ] 如果有效率低于 80%，触发警告
  - [ ] 所有挖到的区块都被网络接受

#### D2. MiningGuard 测试
- [ ] **测试步骤：**
  1. 在未同步状态下尝试挖矿
  2. 在 FOLLOWER 角色下尝试挖矿
  3. 在少于 3 个对等节点时尝试挖矿
  4. 观察 MiningGuard 是否正确阻止

- [ ] **验证点：**
  - [ ] 所有不符合条件的情况都被阻止
  - [ ] UI 显示清晰的阻止原因
  - [ ] 条件满足后能自动恢复挖矿

---

### E. 分叉检测和恢复测试

#### E1. LongRangeDetector 测试
- [ ] **测试步骤：**
  1. 手动修改本地链状态（模拟分叉）
  2. 重新连接网络
  3. 观察 LongRangeDetector 是否检测到分叉
  4. 验证是否建议使用快照修复

- [ ] **验证点：**
  - [ ] 能正确检测到长程分叉
  - [ ] 提供修复建议
  - [ ] 修复后状态正确

#### E2. HeightConsensusManager 测试
- [ ] **测试步骤：**
  1. 本地高度落后网络 5 个区块
  2. 观察是否自动触发快速同步
  3. 本地高度领先网络 5 个区块（模拟分叉）
  4. 观察是否自动停止挖矿

- [ ] **验证点：**
  - [ ] 落后时自动同步
  - [ ] 领先时自动停止挖矿
  - [ ] 显示清晰的警告信息

---

### F. 网络参数验证测试

#### F1. Network Handshake 测试
- [ ] **测试步骤：**
  1. 尝试连接不同 networkId 的节点
  2. 尝试连接不同 genesisHash 的节点
  3. 尝试连接不同 chainParamsHash 的节点

- [ ] **验证点：**
  - [ ] 参数不匹配时自动断开连接
  - [ ] 不会与错误网络的节点通信
  - [ ] UI 显示网络不匹配警告

---

## 🎨 UI 改进 Checklist

### 1. MiningGuard 状态显示
- [ ] **实现：**
  - [ ] 当 MiningGuard 不允许挖矿时，按钮显示具体原因
  - [ ] 原因包括：
    - "🚫 无法挖矿：当前高度落后网络多数，请先同步"
    - "🚫 无法挖矿：本实例为 FOLLOWER，请在 LEADER 页面挖矿"
    - "🚫 无法挖矿：主网参数与信令节点不一致"
    - "🚫 无法挖矿：对等节点数量不足（当前 X / 需要 ≥3）"
    - "🚫 无法挖矿：Finality 未就绪"

### 2. Overview 面板健康状态
- [ ] **实现：**
  - [ ] 添加明显的健康状态指示器
  - [ ] 显示 "✅ Healthy & On Mainnet" 或 "⚠️ Degraded" 或 "🚫 Blocked"
  - [ ] 显示网络模式（Mainnet / Testnet）
  - [ ] 显示同步状态（Synced / Syncing / Out of Sync）

### 3. 挖矿有效性提示
- [ ] **实现：**
  - [ ] 显示挖矿有效率（MiningStats）
  - [ ] 如果有效率低于阈值，显示警告
  - [ ] 显示已接受/拒绝的区块数量

### 4. 同步状态提示
- [ ] **实现：**
  - [ ] 当节点正在从快照同步时，显示 "Node is resyncing from snapshot, mining is disabled until fully synced"
  - [ ] 显示同步进度
  - [ ] 显示预计完成时间

---

## ⚠️ 已知边界条件

### 1. 恶意多数攻击
- **场景：** 如果大多数算力 + 大多数 finality 委员会 + signal 节点一起作恶
- **影响：** 整个网络被 capture
- **说明：** 这已超出协议防护范畴，属于所有链的极限场景（包括 BTC、ETH）

### 2. 用户强制关闭 Guardrails
- **场景：** 手动修改代码，关闭 MiningGuard / HeightConsensus / CheckpointLock
- **影响：** 可能挖出"自己玩的假链"
- **建议：** 在编译时使用常量，开关只在 Dev 模式出现

### 3. 长时间离线后重新上线
- **场景：** 节点离线数天/数周后重新连接
- **影响：** 需要从快照重新同步
- **建议：** UI 明确显示 "Node is resyncing from snapshot, mining is disabled until fully synced"

---

## 📊 性能指标目标

### 同步性能
- [ ] 从快照同步到最新高度：< 30 秒
- [ ] 区块同步延迟：< 5 秒
- [ ] 状态同步延迟（FOLLOWER → LEADER）：< 1 秒

### 挖矿性能
- [ ] 主网挖矿有效率：> 95%
- [ ] 孤块率：< 5%
- [ ] MiningGuard 检查延迟：< 100ms

### 网络性能
- [ ] P2P 连接建立时间：< 3 秒
- [ ] 消息传播延迟：< 2 秒
- [ ] 网络分区检测时间：< 30 秒

---

## 🚀 主网上线前最后检查

### 代码检查
- [ ] 所有 Phase 27-31 功能已实现
- [ ] 所有测试用例通过
- [ ] 代码审查完成
- [ ] 性能测试通过

### 文档检查
- [ ] README.md 更新完成
- [ ] 用户指南更新完成
- [ ] API 文档更新完成

### 部署检查
- [ ] 信令服务器部署完成
- [ ] 主网参数配置正确
- [ ] 监控系统就绪
- [ ] 备份和恢复流程测试完成

---

## 📝 测试报告模板

每次压测后，请填写以下信息：

```
测试日期：YYYY-MM-DD
测试人员：[姓名]
测试环境：[描述]

测试场景：[场景名称]
测试结果：✅ 通过 / ❌ 失败
发现问题：[如有]
修复建议：[如有]

详细日志：[附件或链接]
```

---

## 🎯 总结

以 Phase 27-31 + 主网 guardrails 的设计，只要：
- 客户端是官方版本
- 连接的是主网 signal
- P2P 网络中 honest 节点占多数

那"不同浏览器高度/余额不同步"和"用户白挖"这两个问题，架构上已经被系统性解决了。

**后续重点：压测 + UX 提示做得足够清楚。**
