# 余额同步问题诊断指南

## 问题描述

不同浏览器显示相同地址的余额不一致：
- 谷歌浏览器：地址 `idc_b883fa50c2de95302ca3ddc0fe43c3b4b4276416` 余额为 2983.63 IDC
- 苹果浏览器：相同地址余额显示为 0 IDC

## 可能的原因

### 1. 区块数量不同步 ⚠️ **最可能的原因**

每个浏览器有独立的 `localStorage`，如果：
- 一个浏览器连接到了 P2P 网络并同步了更多区块
- 另一个浏览器没有连接或连接失败
- 两个浏览器连接到了不同的网络（主网 vs 开发网）

**解决方案**：
1. 检查两个浏览器的区块高度是否一致
2. 确保两个浏览器都连接到同一个 P2P 网络
3. 使用"强制同步区块"按钮手动触发同步

### 2. P2P 连接状态不同

**检查方法**：
- 打开"余额诊断"面板（在钱包状态卡片中）
- 查看"P2P 连接"状态
- 如果显示"未连接"或"未初始化"，需要先连接 P2P 网络

**解决方案**：
1. 前往 "Network" 标签页
2. 确保"Mainnet Mode"已启用（或使用相同的信令服务器）
3. 点击"连接"按钮
4. 等待连接到其他节点

### 3. 状态重建不完整

如果浏览器刷新后状态没有正确重建：
- 可能使用了不完整的快照
- 可能某些区块在重建时被跳过

**解决方案**：
1. 检查"区块数量"是否与"区块高度"匹配
2. 如果数量不一致，可能需要重置链（⚠️ 会丢失本地数据）

## 诊断步骤

### 步骤 1: 检查余额诊断信息

1. 在两个浏览器中打开 IndexerChain
2. 前往 "Overview" 标签页
3. 在"钱包状态"卡片中，点击"🔍 余额诊断"
4. 对比以下信息：
   - **区块高度**：应该相同
   - **区块数量**：应该相同
   - **P2P 连接**：应该都显示"已连接"
   - **余额 (balances namespace)**：应该相同

### 步骤 2: 检查 P2P 连接

1. 前往 "Network" 标签页
2. 检查两个浏览器的：
   - 连接状态
   - 节点数量
   - 信令服务器地址（应该相同）

### 步骤 3: 强制同步

如果诊断显示区块高度不一致：

1. 在余额较低的浏览器中：
   - 打开"余额诊断"面板
   - 点击"🔄 强制同步区块"按钮
   - 等待同步完成（可能需要几分钟）

2. 检查控制台日志：
   - 打开浏览器开发者工具（F12）
   - 查看 Console 标签页
   - 查找 `[Sync]` 或 `[P2P]` 相关的日志

### 步骤 4: 验证同步结果

同步完成后：
1. 刷新页面
2. 再次检查余额诊断信息
3. 确认两个浏览器的余额是否一致

## 常见问题

### Q: 为什么两个浏览器显示不同的区块高度？

**A**: 这通常意味着：
- 一个浏览器没有连接到 P2P 网络
- 或者两个浏览器连接到了不同的网络（主网 vs 开发网）

**解决方法**：确保两个浏览器都连接到同一个信令服务器。

### Q: 强制同步后余额仍然不一致？

**A**: 可能的原因：
1. 两个浏览器在不同的分叉上
2. 一个浏览器有本地挖出的区块，另一个没有

**解决方法**：
1. 检查"最新区块哈希"是否相同
2. 如果不同，说明在两个不同的分叉上
3. 需要等待网络收敛到最长链

### Q: 可以手动导出/导入区块数据吗？

**A**: 当前版本不支持直接导出/导入区块数据。建议：
1. 确保两个浏览器都连接到 P2P 网络
2. 让网络自动同步区块
3. 如果问题持续，可能需要重置链（⚠️ 会丢失本地数据）

## 技术细节

### 余额存储位置

余额存储在 `IndexState` 的 `"balances"` namespace 中：
- Key: 地址（如 `idc_b883fa50c2de95302ca3ddc0fe43c3b4b4276416`）
- Value: 余额（字符串格式，如 `"2983.63"`）

### 余额计算方式

余额通过重放所有区块中的交易计算：
1. 从创世块开始
2. 应用每个区块中的所有交易
3. Coinbase 交易增加矿工余额
4. TRANSFER 交易更新发送者和接收者余额

### 状态重建

`IndexState.rebuildFromBlocks()` 方法：
1. 清空当前状态
2. 按顺序应用所有区块
3. 每个区块中的交易会更新余额

## 预防措施

1. **始终连接到 P2P 网络**：确保浏览器连接到主网或开发网
2. **定期检查同步状态**：使用余额诊断工具检查区块高度
3. **避免在多个浏览器中同时挖矿**：使用 Phase 27 的本地实例协调功能

## 如果问题仍然存在

如果按照上述步骤操作后问题仍然存在，请：

1. **收集诊断信息**：
   - 两个浏览器的区块高度
   - 两个浏览器的区块数量
   - 两个浏览器的 P2P 连接状态
   - 两个浏览器的最新区块哈希

2. **检查控制台日志**：
   - 查找错误信息
   - 查找同步相关的日志

3. **考虑重置链**（最后手段）：
   - ⚠️ **警告**：这会清除所有本地数据
   - 前往 "Advanced" 标签页
   - 点击"重置链"按钮
   - 重新连接到 P2P 网络同步

